# Handoff — 2026-09-02 · Phase 3 done (outcome scoring loop + hooks)

**From:** Implementor (Claude Code session) · **To:** next session / Phase 4
**Repo:** `D:\Development\ai-toolbox`, branch `ai-toolbox`
**Commit:** `config: outcome scoring loop + hooks (v0.4.0)` (see `git log` for hash)

---

## TL;DR

- Phase 3 of `docs/he/WorkPlan.md` (§שלב 3) is complete: `rules/05-outcome-scoring.md`,
  `skills/toolbox-outcome/SKILL.md` + `commands/outcome.md`, `SubagentStop`/`Stop`
  hooks reminding to close an open `decision`, and `verified` is now genuinely
  computed from `score.outcome` (a real gap closed, not just docs).
- `scripts/rescore.py` from the PRD's file layout was **not** created — the
  score fold it would compute already lives in `store.py`'s `_fold_scores()`/
  `_project_tools()` (built in Phase 2, tested in `test_contract.py`). Adding
  a second file would duplicate the fold, which P3 explicitly warns against.
  Reconciled `docs/PRD.md` §5.1 and §7.5 to say so instead of building a
  second implementation of the same logic.
- `python scripts/validate.py`, `python scripts/backends/test_contract.py`,
  and the new `python hooks/test_remind_outcome.py` all pass clean.

---

## What changed

| File | Notes |
|---|---|
| `rules/05-outcome-scoring.md` | Decision 5: what to append (`score.outcome`/`score.human`), the fold `store project` computes (min-samples, EMA+decay, trend, **verified**, retraction), regret analysis (monthly, auditor's job), one worked numeric example carrying a tool from `estimate: true` (4 samples) to a real EMA (5th sample, a `score.human`) |
| `skills/toolbox-outcome/SKILL.md` | 8-step procedure: find the decision → gather per-tool outcome → append `score.outcome` → `score.human` for human scores → escalation → `score.retract` for corrections → `store project` → note on Validator auto-recording for L3 (`actor: auto:validator`, same event shape, no separate agent file needed since none exists yet) |
| `commands/outcome.md`, `commands/project.md` | Thin wrappers — `outcome` invokes the skill, `project` re-folds and diffs the views before staging (3.3b) |
| `hooks/hooks.json` | `SubagentStop` + `Stop` → `hooks/remind_outcome.py` |
| `hooks/remind_outcome.py` | Queries `store query scores --filter kind=decision` and `kind=score.outcome`/`score.human`, diffs the sets, reminds once per decision (state tracked in a local, gitignored `.reminded.json` — ponytail-flagged: single small state file, not a queue, upgrade if per-session dedup isn't enough) |
| `hooks/test_remind_outcome.py` | Self-check: open-decision detection + once-only reminding, against a temp `--base-dir` via `TOOLBOX_BASE_DIR` |
| `scripts/store.py` | `_fold_scores()` now also computes `verified` (date of the latest successful `score.outcome`) per `docs/schema.md`'s stated behavior — this was documented but never implemented before this phase. `_project_tools()` merges it with any migration-era `tool.status`-set `verified` by taking the max date, so Phase 1's migrated dates aren't lost. |
| `docs/PRD.md` | §5.1 file layout and §7.5/P3 rows reconciled to drop the separate `rescore.py` — the fold already lives in `store.py` |
| `.gitignore` | `hooks/.reminded.json` (local hook state, never committed) |
| `catalog/views/scores-summary.yaml` | Re-projected after the `verified` fold change — every tool got a `verified: null` entry (no real `score.outcome` events exist yet in the migrated catalog, so this is a no-op on today's data, correct once real outcomes start landing) |

## Scope notes / what's NOT done

- **3.5's acceptance test** ("בדיקה על משימת L3") wasn't run against a real
  L3 task — there isn't one in flight this session. The mechanism (skill
  step 8: Validator appends `score.outcome` with `actor: auto:validator`)
  is in place and documented; verify it end-to-end the next time an L3 task
  actually runs through `toolbox-route` → swarm → Validator.
- `SessionStart`/`UserPromptSubmit` hooks (routable-tool-count / L0–L3
  nudge) are in the PRD's hooks table but were never assigned to a WorkPlan
  task — Phase 3's own task list (3.4) only asked for `SubagentStop`+`Stop`.
  Left unbuilt; flag if a later phase should pick them up.
- Regret analysis (§C of the new rule) is written up but has no runnable
  code yet — it's explicitly the **auditor**'s job (Phase 4, `agents/toolbox-auditor.md`,
  not built yet), so this phase only documents what it should do.

## Verification results

```
python scripts/validate.py                  → all checks passed
python scripts/backends/test_contract.py    → all contract tests passed (9 × 2 backends = 18/18)
python hooks/test_remind_outcome.py         → both checks pass
```

## What's next — Phase 4 (curator/assessor/auditor agents)

Per `docs/he/WorkPlan.md` §שלב 4: `skills/toolbox-curate/SKILL.md`, the three
agent role files (`agents/toolbox-curator.md`, `toolbox-assessor.md`,
`toolbox-auditor.md` — the auditor is also who'd eventually run 05's regret
analysis for real), the three `.github/workflows/*.yml`, `ops/` migration,
`commands/add.md`+`commands/audit.md`.

Also still open from Phase 1/2 (unaffected by this phase): `task_types`
tagging gap on all 72 migrated tools, and `rules/02-orchestrator-model.md`'s
"minus one" tier ambiguity for agent-free L1 tasks (see
`handoffs/2026-09-02-phase2-done.md`).
