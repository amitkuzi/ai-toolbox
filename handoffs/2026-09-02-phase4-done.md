# Handoff — 2026-09-02 · Phase 4 done (curator/assessor/auditor agents + scheduled runs)

**From:** Implementor (Claude Code session) · **To:** next session / Phase 5  
**Repo:** `D:\Development\ai-toolbox`, branch `ai-toolbox`  
**Commit:** `team: curator/assessor/auditor agents + scheduled runs (v0.5.0)` (see `git log` for hash)

---

## TL;DR

- Phase 4 of `docs/he/WorkPlan.md` (§שלב 4) is complete: three agent roles (curator, assessor, auditor),
  the `toolbox-curate` skill with daily/weekly routines, three GitHub Actions workflows, Docker/ops
  for self-hosted scheduling, and manual commands (`/toolbox:add`, `/toolbox:audit`).
- All agents are T1/T2 read-only on catalog data; they append events only through `store.py`.
- Notification infrastructure (ntfy.sh + Slack) is in place but requires Phase 5 to integrate
  with the actual Claude API for agent execution.
- `python scripts/validate.py`, append-only guard, and views projection all pass.

---

## What changed

| Item | Path | Notes |
|---|---|---|
| **Skill** | `skills/toolbox-curate/SKILL.md` | 8-step daily (discover, validate, close gaps) + 5-step weekly (re-score sources, hunt new ones) |
| **Curator agent** | `agents/toolbox-curator.md` | T1, max-turns 40, tools: Read/Bash/WebSearch/WebFetch (no Edit) |
| **Assessor agent** | `agents/toolbox-assessor.md` | T2, structural scoring rubric, writes `evals/<id>.md`, never executes tools |
| **Auditor agent** | `agents/toolbox-auditor.md` | T2, monthly audit: stale tools, licenses, regret analysis, proposes PR |
| **GitHub workflows** | `.github/workflows/{daily-refresh,weekly-sources,monthly-audit}.yml` | Scheduled on cron; failure alerts via `notify.sh` |
| **Docker setup** | `ops/{Dockerfile,docker-compose.yml,run-task.sh}` | Self-hosted scheduling; `docker compose run --rm runner` or local cron |
| **Notifications** | `ops/{notify.sh,notifications.md}` | Ntfy.sh (default, no-auth) or Slack webhooks for failure alerts |
| **Commands** | `commands/{add.md,audit.md}` | `/toolbox:add <id>` and `/toolbox:audit` for manual triggers |

## Scope notes / what's NOT done

- **Agent execution via Claude API:** The workflows and `run-task.sh` currently stub out the actual
  agent invocation (Phase 5 will add `CLAUDE_API_KEY` + agent spawning logic).
- **Real tool discovery:** Workflows project the views and validate, but don't yet call curator/assessor/auditor
  agents (that's Phase 5: "Agent execution via Claude Code").
- **PR automation for auditor:** Monthly audit creates a draft PR but requires manual intervention to merge.
  Fully automated merges would need additional CI/CD setup (Phase 6+).

## Verification results

```
python scripts/validate.py                 → all checks passed
git status                                 → clean
git log --oneline -5                       → shows v0.5.0 at HEAD
```

## What's next — Phase 5 (documentation + packaging)

Per `docs/he/WorkPlan.md` §שלב 5 (1.5 days):
- Write `docs/PRD.md`, `docs/architecture.md`, `docs/rules.md`, `docs/curator.md`
- Write `docs/customer-guide.md` (installation steps)
- Update `README.md` with GIF of `/toolbox:route` in action
- Catalog separation: move real catalog to private `ai-toolbox-catalog` repo, ship `catalog-example/` publicly
- Create `docs/storage.md` (Store contract + how to write backends)
- Point skills repo (`Skills/ai-toolbox/SKILL.md`) to this plugin
- Write `CHANGELOG.md` and `docs/lecture-kit.md` (slides ↔ code linkage)
- License audit (Phase 5.7)

Also still open from Phases 1–3: `task_types` tagging on 72 migrated tools (gap persists), and
`rules/02-orchestrator-model.md` "minus one" tier ambiguity (flagged in Phase 2 handoff).

---

## Git and branch status

- Working branch: `ai-toolbox`
- Remote: up-to-date
- Uncommitted changes: none
- Version in commit: v0.5.0

---

## Notes for Phase 5 implementor

- **Agent invocation:** When Phase 5 adds Claude Code integration, each agent file (curator, assessor, auditor)
  will be invoked by the workflows/`run-task.sh` via the Claude API; add `--max-turns N` and `--model` flags
  per each agent's documented tier.
- **Tool allowlist:** Each agent has an explicit ✅/❌ tool list in their `.md` file; enforce this via agent
  config (no `Edit` on ledger/views).
- **Notification secrets:** GitHub Actions workflows will need `CLAUDE_API_KEY` (in secrets) and optional
  `NOTIFY_URL`/`SLACK_WEBHOOK` for alerts. Update `.github/workflows/*.yml` `env:` section.
- **Real sources:** The curator will need real tool sources added to `catalog/ledger/sources.jsonl` before
  the daily run can discover anything. Stub a few high-confidence sources (PyPI, GitHub Trending, ArXiv)
  in Phase 4's discovery.

---

## See also

- Handoff from Phase 3: `handoffs/2026-09-02-phase3-done.md`
- Work plan: `docs/he/WorkPlan.md` §שלב 4 (done) and §שלב 5 (next)
- Agent files: each `.md` has a "See also" section linking to relevant rules and skills
